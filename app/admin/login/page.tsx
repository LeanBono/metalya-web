'use client'; import {FormEvent,useState} from 'react'; import {useRouter} from 'next/navigation';
export default function Login(){const [p,setP]=useState(''),[e,setE]=useState('');const r=useRouter();async function go(x:FormEvent){x.preventDefault();setE('');const res=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password:p})});if(res.ok)r.push('/admin');else setE('Contraseña incorrecta.');}return <main className="adminWrap"><div className="adminLogin"><a className="brand" href="/"><span className="mark">M</span><span>Metal<span>Ya</span></span></a><h1>Panel MetalYa</h1><p>Acceso administrativo.</p><form onSubmit={go}><label>Contraseña<input type="password" value={p} onChange={e=>setP(e.target.value)} required autoFocus/></label>{e&&<div className="error">{e}</div>}<button className="btn primary">Ingresar</button></form></div></main>}
