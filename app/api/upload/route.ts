import { NextResponse } from 'next/server'; import { prisma } from '@/lib/prisma'; import { randomUUID } from 'crypto'; import { writeFile, mkdir } from 'fs/promises'; import path from 'path';
export const runtime='nodejs';
export async function POST(req:Request){try{const form=await req.formData();const leadId=String(form.get('leadId')||'');if(!leadId)return NextResponse.json({error:'Falta leadId'},{status:400});const lead=await prisma.lead.findUnique({where:{id:leadId},include:{lots:true}});if(!lead?.lots[0])return NextResponse.json({error:'Solicitud inexistente'},{status:404});const files=form.getAll('photos').filter(x=>x instanceof File) as File[];if(files.length>6)return NextResponse.json({error:'Máximo 6 fotos.'},{status:400});const dir=path.join(process.cwd(),'public','uploads');await mkdir(dir,{recursive:true});const urls=[];for(const file of files){if(!['image/jpeg','image/png','image/webp'].includes(file.type)||file.size>5*1024*1024)continue;const ext=file.type==='image/jpeg'?'jpg':file.type.split('/')[1];const name=`${randomUUID()}.${ext}`;await writeFile(path.join(dir,name),Buffer.from(await file.arrayBuffer()));const url=`/uploads/${name}`;await prisma.lotPhoto.create({data:{lotId:lead.lots[0].id,url,alt:`Foto de lote de ${lead.name}`}});urls.push(url)}return NextResponse.json({ok:true,urls});}catch(e){console.error(e);return NextResponse.json({error:'No se pudieron guardar las fotos.'},{status:500});}}
